# Agent Instructions (Local_Codex_appserver)

Documentation discipline:
- Follow `../doc/DOCS_RULES.md`.
- Keep `doc/INDEX.md` updated when creating/updating documentation files.

Workflow expectation:
- Before implementation changes, create/update SPEC + TODO (+ minimal test plan if relevant).
- During implementation, record important decisions in docs (SPEC or DECISIONS).
