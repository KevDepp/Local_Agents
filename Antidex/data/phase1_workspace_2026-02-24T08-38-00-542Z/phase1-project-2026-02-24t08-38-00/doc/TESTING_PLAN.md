# Testing Plan

Checklist:
- [ ] After T-001: `hello.txt` exists (e.g. `Test-Path .\\hello.txt`).
- [ ] After T-002: `world.txt` exists (e.g. `Test-Path .\\world.txt`).
- [ ] After T-003: `files.md` exists and mentions both `hello.txt` and `world.txt` (e.g. `Get-Content .\\files.md`).
