# Decisions

- 2026-02-18: Defined `smoke_project` as a planning-only validation run and created initial planning docs, the first task `T-001_smoke_planning`, and a dispatching entry in `data/pipeline_state.json`. (rationale: validate the Antidex Manager → Developer pipeline without touching application code)
