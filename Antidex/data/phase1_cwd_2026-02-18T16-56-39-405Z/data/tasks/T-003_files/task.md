# Task T-003_files — Create `files.md` listing `hello.txt` and `world.txt`

User story:
- As a user, I want a Markdown file `files.md` that lists the previously created files `hello.txt` and `world.txt`.

Requirements:
- Create a new Markdown file `files.md` at the project root.
- The content must clearly list both `hello.txt` and `world.txt` (for example as bullet points).
- Assume `hello.txt` and `world.txt` already exist from previous tasks; do not recreate, modify, or delete them.
- Keep changes minimal and focused on this file.

Definition of Done:
- `files.md` exists at the project root.
- `files.md` mentions both `hello.txt` and `world.txt`.
- `dev_result` includes the exact commands used to create and verify the file (e.g. `Test-Path files.md`, `Get-Content files.md`) and their outputs.
- No unrelated files are modified.
- All instructions in `manager_instruction.md` are followed.

Developer:
- `developer_codex`

