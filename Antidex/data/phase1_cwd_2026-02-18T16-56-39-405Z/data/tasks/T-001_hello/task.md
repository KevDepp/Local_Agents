# Task T-001_hello — Create `hello.txt`

User story:
- As a user, I want a file `hello.txt` created at the project root as the first step of a simple file pipeline.

Requirements:
- Create a new text file named `hello.txt` at the project root.
- Do not create `world.txt` or `files.md` in this task.
- Keep changes minimal and focused on this file.

Definition of Done:
- `hello.txt` exists at the project root.
- `dev_result` includes the exact commands used to create and verify the file (e.g. `Test-Path hello.txt`) and their outputs.
- No unrelated files are modified.
- All instructions in `manager_instruction.md` are followed.

Developer:
- `developer_codex`

