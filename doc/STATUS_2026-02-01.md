# Statut (2026-02-01)

## Ce qui a été fait
- Création d’un dossier `Local_Agents/doc/` + documentation initiale.
- POC d’extension `Local_Agents/prompt-bridge/` :
  - serveur HTTP local (`/health`, `/commands`, `/send`)
  - envoi best-effort vers Codex (`chatgpt.*`) ou Antigravity (`antigravity.*`)
  - commandes VS Code : `Prompt Bridge: Diagnostics`, `Prompt Bridge: Show listening port`
  - compilation TypeScript OK + lint OK
  - scripts “minimum de manipulations” : `scripts/quickstart.ps1` + `scripts/selftest.ps1`
- POC RPA `Local_Agents/rpa-chat/` :
  - robot `send_to_chats.robot` (focus → paste → enter)
  - exécution en `--dryrun` OK

## Ce qui fonctionne (testé ici)
- `prompt-bridge`: `npm install`, `npm run compile` OK.
- `prompt-bridge`: `npm run lint` OK.
- `rpa-chat`: `robot --dryrun` via `.\smoke.ps1` OK.

## Ce qui manque / points ouverts
- Validation “end-to-end” réelle (attended) du robot : dépend de tes fenêtres ouvertes + keybindings + focus dans la WebView Codex.
- Confirmation des `command IDs` réellement disponibles dans TA version d’Antigravity (via `GET /commands?filter=antigravity` ou `Prompt Bridge: Diagnostics`).
- Choix/standardisation des ports : éviter collision quand VS Code + Antigravity tournent en même temps (ex: 17373 / 17374).

## Notes
- `npm install` a signalé des vulnérabilités modérées dans des dépendances de dev (eslint/anciennes transitive deps). Pas critique pour un POC local, mais à nettoyer si tu veux le publier.
