{
  "name": "ui-test-prompt",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal UI prompt runner with Playwright tests.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1"
  }
}
