# Manager Instruction — T-001_hello

Role: developer_codex

Veuillez:
- Lire `agents/developer_codex.md` (et vérifier la version).
- Lire `data/tasks/T-001_hello/task.md` pour le détail de la tâche et la Definition of Done.

Travail attendu:
- Créer le fichier `hello.txt` à la racine du projet conformément aux exigences.
- Ne pas créer `world.txt` ni `files.md` dans cette tâche.
- Documenter:
  - un ACK dans `data/tasks/T-001_hello/dev_ack.json` (au format simple de votre choix, par ex. `{ \"status\": \"ack\" }`),
  - les résultats et preuves (commandes + sorties) dans `data/tasks/T-001_hello/dev_result.md` (ou `.json`).

Tests:
- Fournir au minimum une commande de vérification (ex: `Get-Content hello.txt`) et sa sortie.

Rappels:
- Pas de commit Git sans instruction explicite du Manager (voir `doc/GIT_WORKFLOW.md`).

