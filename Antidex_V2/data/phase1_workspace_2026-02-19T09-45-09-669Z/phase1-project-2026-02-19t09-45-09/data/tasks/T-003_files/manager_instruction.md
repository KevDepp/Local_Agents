# Manager Instruction — T-003_files

Role: developer_codex

Veuillez:
- Lire `agents/developer_codex.md`.
- Lire `data/tasks/T-003_files/task.md` pour le détail de la tâche et la Definition of Done.

Travail attendu:
- Créer `files.md` en Markdown listant `hello.txt` et `world.txt` à la racine du projet, sans modifier le contenu de ces deux fichiers.
- Documenter:
  - un ACK dans `data/tasks/T-003_files/dev_ack.json`,
  - les résultats et preuves (commandes + sorties) dans `data/tasks/T-003_files/dev_result.md` (ou `.json`).

Tests:
- Fournir au minimum une commande de vérification (ex: `Get-Content files.md`) et sa sortie.

Rappels:
- Pas de commit Git sans instruction explicite du Manager.

