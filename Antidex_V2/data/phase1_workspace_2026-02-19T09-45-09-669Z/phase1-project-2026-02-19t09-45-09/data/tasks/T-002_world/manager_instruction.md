# Manager Instruction — T-002_world

Role: developer_codex

Veuillez:
- Lire `agents/developer_codex.md`.
- Lire `data/tasks/T-002_world/task.md` pour le détail de la tâche et la Definition of Done.

Travail attendu:
- Créer le fichier `world.txt` à la racine du projet, sans modifier `hello.txt` et sans créer `files.md`.
- Documenter:
  - un ACK dans `data/tasks/T-002_world/dev_ack.json`,
  - les résultats et preuves (commandes + sorties) dans `data/tasks/T-002_world/dev_result.md` (ou `.json`).

Tests:
- Fournir au minimum une commande de vérification (ex: `Get-Content world.txt`) et sa sortie.

Rappels:
- Pas de commit Git sans instruction explicite du Manager.

