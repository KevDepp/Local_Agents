Summary  
- Added AG ACK fallback logic: treat ACK as best‑effort when AG filesystem activity is detected, and auto‑resend once on the first ACK timeout with no activity before handing off to Manager.  
- Updated docs to reflect the new ACK behavior and patterns (`doc/SPEC.md`, `doc/TODO.md`, `doc/TESTING_PLAN.md`, `doc/DECISIONS.md`, `doc/CORRECTOR_FIX_PATTERNS.md`, `doc/INDEX.md`).

Tests  
- `node -e "require('./server/pipelineManager.js'); console.log('ok')"`