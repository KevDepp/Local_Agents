# Testing Plan

Checklist:
- [ ] After T-001_hello: verify `hello.txt` exists and content matches task definition.
- [ ] After T-002_world: verify `world.txt` exists and content matches task definition.
- [ ] After T-003_files: verify `files.md` exists and lists both `hello.txt` and `world.txt` per task definition.
- [ ] For each task, capture proof of existence/content in `dev_result` and/or test snippets.
