# Task T-002_world — Create `world.txt`

User story:
- As a user, I want a file `world.txt` created at the project root as the second step of a simple file pipeline.

Requirements:
- Create a new text file named `world.txt` at the project root.
- Assume `hello.txt` may already exist from `T-001_hello`; do not modify or delete it.
- Do not create `files.md` in this task.
- Keep changes minimal and focused on this file.

Definition of Done:
- `world.txt` exists at the project root.
- `dev_result` includes the exact commands used to create and verify the file (e.g. `Test-Path world.txt`) and their outputs.
- No unrelated files are modified.
- All instructions in `manager_instruction.md` are followed.

Developer:
- `developer_codex`

