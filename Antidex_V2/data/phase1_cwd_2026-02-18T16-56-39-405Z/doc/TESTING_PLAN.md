# Testing Plan

Last updated: 2026-02-18.

Checklist:
- [ ] Apres `T-001_hello`: verifier que `hello.txt` existe a la racine du projet (ex: `Test-Path hello.txt`) et que le developpeur a fourni la commande + la sortie dans `dev_result`.
- [ ] Apres `T-002_world`: verifier que `world.txt` existe a la racine du projet (ex: `Test-Path world.txt`) et que le developpeur a fourni la commande + la sortie dans `dev_result`.
- [ ] Apres `T-003_files`: verifier que `files.md` existe a la racine du projet et que son contenu mentionne `hello.txt` et `world.txt` (ex: inspection manuelle ou `Select-String`).
